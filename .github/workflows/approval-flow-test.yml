name: Approval flow test

on:
  workflow_dispatch:

jobs:
  gated:
    runs-on: ubuntu-latest
    environment: production-publish
    steps:
      - run: echo "Approved - this is where the real release publish would happen."
